/**
 * Used when requesting to generate data in the server
 * with a text response.
 *
 * @export
 * @interface ISimpleGeneratorResultDto
 */
export interface ISimpleGeneratorResultDto {
    resultText: string;
}