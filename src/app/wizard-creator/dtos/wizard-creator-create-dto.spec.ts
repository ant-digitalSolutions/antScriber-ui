import { WizardCreatorCreateDto } from './wizard-creator-create-dto';

describe('WizardCreatorCreateDto', () => {
  it('should create an instance', () => {
    expect(new WizardCreatorCreateDto()).toBeTruthy();
  });
});
